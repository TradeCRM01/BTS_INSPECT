# SPA fallback for client-side routes (Cloudflare Pages).
# Do not add a top-level 404.html — it disables Pages' SPA behavior.
# Do not use status-404 rewrites here — they are unsupported and can invalidate this file.
/*    /index.html   200
